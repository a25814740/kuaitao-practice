# STAGE_CHECKLIST

> 目的：把每個 stage 拆成更容易開始的小步驟。
> 使用方式：每完成一小段就打勾，不用一次把整個 stage 做完。

## 使用原則

- 一次只專注一個 stage，不跳級。
- 每個 stage 至少完成「能跑、能看、能說明」三件事。
- 每做完一個 stage，要同步更新 `docs/DEVLOG.md`。
- 如果某一步卡住，可以先做下一個更小的 UI 或資料步驟，再回頭補。

---

## Stage 1｜Vue 3 基礎響應式

### 目標

- 建立首頁單頁雛形。
- 透過按鈕記錄一次「塊陶啊!!」的時間。
- 理解 `ref`、`@click`、模板綁定。

### 子步驟

- [x] 清掉 `App.vue` 預設內容，換成自己的頁面骨架。
- [x] 寫出標題、副標題、主要按鈕。
- [x] 建立 `ref` 狀態，例如 `lastEscapeTime`、`clickCount`。
- [x] 建立點擊函式，點擊後更新狀態。
- [x] 在畫面上顯示最後點擊時間與次數。
- [x] 補上簡單樣式，讓首頁不再是純預設頁。
- [x] 用 `npm run dev` 確認畫面可正常操作。
- [x] 更新 `docs/DEVLOG.md`。

### 驗收

- [x] 點擊按鈕後畫面會即時更新。
- [x] 能說清楚 `ref` 與事件綁定在做什麼。
- [x] 專案可正常啟動。

---

## Stage 2｜條件渲染與計算邏輯

### 目標

- 顯示距離下班的倒數。
- 根據目前時間切換狀態。
- 理解 `computed`、`watch`、`v-if`。

### 子步驟

- [x] 先寫死預設下班時間（`OFF_WORK_HOUR` / `OFF_WORK_MINUTE`）。
- [x] 建立目前時間狀態 `now`，用 `setInterval` 每秒更新。
- [x] 用 `computed` 算出下班時間 `offWorkTime` 與差距 `diffMs`。
- [x] 用 `computed` 判斷狀態 `status`（`before` / `on` / `after`）。
- [x] 用 `computed` 產出倒數顯示文字（`diffText`、`countdownLabelText`、`statusText`）。
- [x] 用 `:class` / `v-if` 做狀態切換（加班時變紅、結果區塊有資料才顯示）。
- [x] 點擊後記錄當下時間，並顯示與預設下班時間的差距（提早 / 加班多久）。
- [x] 整理條件邏輯，避免在模板塞太多計算。
- [x] 更新 `docs/DEVLOG.md`。

### 驗收

- [x] 倒數會持續變化（每秒更新）。
- [x] 不同狀態會顯示不同文案或樣式（例如加班紅色提示）。
- [x] 能說清楚 `computed` 和 `watch` 的差別（本階段主要用 `computed` 完成）。

---

## Stage 3｜元件化

### 目標

- 把首頁拆成多個可理解的小元件。
- 理解 `props`、`emits`、父子元件資料流。

### 子步驟

- [ ] 盤點首頁有哪些區塊適合拆元件。
- [ ] 建立 `TimeDisplay.vue`。
- [ ] 建立 `ResultCard.vue`。
- [ ] 建立 `EscapeButton.vue`。
- [ ] 建立 `RecordHistory.vue`。
- [ ] 用 `defineProps` 傳遞顯示資料。
- [ ] 用 `defineEmits` 傳遞按鈕互動事件。
- [ ] 整理 `App.vue`，只保留頁面組裝邏輯。
- [ ] 更新 `docs/DEVLOG.md`。

### 驗收

- [ ] `App.vue` 明顯變短、結構更清楚。
- [ ] 子元件責任單純。
- [ ] 能說清楚父傳子、子傳父流程。

---

## Stage 4｜Composables 與邏輯抽離

### 目標

- 把時間與狀態邏輯抽出元件。
- 理解 composable 的用途與邏輯分層。

### 子步驟

- [ ] 找出與畫面無關的邏輯。
- [ ] 建立 `useEscapeTimer`。
- [ ] 建立 `useOffDutyStatus`。
- [ ] 搬移計時器與狀態判斷邏輯。
- [ ] 在頁面重新接回 composable 回傳資料。
- [ ] 清理不再需要的重複程式。
- [ ] 確認畫面行為與前一階段一致。
- [ ] 更新 `docs/DEVLOG.md`。

### 驗收

- [ ] composable 可讀性清楚。
- [ ] 元件內邏輯明顯減少。
- [ ] 能說明為什麼這些邏輯適合抽出去。

---

## Stage 5｜Pinia 全域狀態

### 目標

- 建立使用者設定資料的共享狀態。
- 讓首頁與設定頁讀同一份資料。

### 子步驟

- [ ] 定義需要共享的使用者欄位。
- [ ] 建立 `user` store。
- [ ] 設定 store 預設值。
- [ ] 建立設定頁表單欄位。
- [ ] 設定頁寫入 store。
- [ ] 首頁讀取 store 顯示個人化內容。
- [ ] 整理哪些狀態要放 store、哪些保留元件內。
- [ ] 更新 `docs/DEVLOG.md`。

### 驗收

- [ ] 不同頁面能共用同一份資料。
- [ ] 修改設定後首頁內容同步更新。
- [ ] 能說清楚 Pinia 和 local state 的差異。

---

## Stage 6｜Vue Router 與多頁面

### 目標

- 建立基本多頁架構。
- 理解 router、view、layout 的分工。

### 子步驟

- [ ] 規劃頁面與網址路徑。
- [ ] 建立 `views` 資料夾與各頁骨架。
- [ ] 設定 `router/index.ts` 路由表。
- [ ] 在 `App.vue` 放入 `<RouterView />`。
- [ ] 建立簡單導覽列。
- [ ] 把首頁與設定頁接回既有功能。
- [ ] 測試切頁與重整行為。
- [ ] 更新 `docs/DEVLOG.md`。

### 驗收

- [ ] 各頁都能正常切換。
- [ ] 頁面架構清楚，不再全部擠在同一頁。
- [ ] 能說清楚 SPA 的切頁概念。

---

## Stage 7｜假資料與列表實作

### 目標

- 用 mock data 做出完整產品感。
- 練習清單、排序、篩選與假資料管理。

### 子步驟

- [ ] 建立排行榜 mock data。
- [ ] 建立社交牆 mock data。
- [ ] 建立成就 mock data。
- [ ] 完成排行榜列表與排序切換。
- [ ] 完成社交牆貼文卡片與留言假畫面。
- [ ] 完成成就頁列表與狀態標記。
- [ ] 補上篩選條件與空狀態。
- [ ] 調整版型，提升展示感。
- [ ] 更新 `docs/DEVLOG.md`。

### 驗收

- [ ] 沒有 API 也能展示完整功能流程。
- [ ] 清單、排序、篩選都能正常運作。
- [ ] 能說明 mock data 為什麼先做。

---

## Stage 8｜Supabase 串接

### 目標

- 把前面假資料版本升級成真實資料版本。
- 練習 CRUD、非同步資料流、錯誤處理。

### 子步驟

- [ ] 建立 Supabase 專案與環境變數。
- [ ] 串接使用者設定資料表。
- [ ] 串接下班紀錄資料表。
- [ ] 把排行榜改為真實資料來源。
- [ ] 把貼文與留言改成真實 CRUD。
- [ ] 加入 loading、error、empty state。
- [ ] 檢查資料流程與 UI 是否一致。
- [ ] 視情況加入 auth。
- [ ] 更新 `docs/DEVLOG.md`。

### 驗收

- [ ] 至少一組核心資料已完成真實儲存與讀取。
- [ ] 畫面可處理載入中、失敗、無資料三種狀態。
- [ ] 能說明從 mock data 過渡到 API 的差異。

---

## 通用收尾檢查

- [ ] `npm run dev` 可正常啟動。
- [ ] 本階段功能可實際操作。
- [ ] `docs/DEVLOG.md` 已更新。
- [ ] `docs/STAGE_CHECKLIST.md` 已更新。
- [ ] 能用自己的話說明本階段學到的 Vue 3 概念。
